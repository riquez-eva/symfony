{% extends 'base.html.twig' %}

{% block body %}

<h1>Liste des disques</h1>

    <div class="col-md-6">
    <table class="table table-bordered">
        <thead>
        <tr>
            <th>Couverture d'Album</th>
            <th>Artiste</th>
        </tr>
        </thead>
        <tbody>
{% for disc in discs %}

        <!-- Tableau-->
        <tr>
            <td>
            <div class="">
               <img src="{{ asset('pictures/'~ disc.picture ) }}" alt="{{ disc.title }}" style="width: 250px; height: auto" srcset=""> 
               </div>
            </td>
            <td>
                <h4>{{ disc.title}}</h4> <br>
               Name : {{disc.artist.name}}<br>
               Label : {{disc.label}}<br>
               Year : {{disc.year}} <br>
               Genre : {{disc.genre}} <br>
               <a href="#">Détails</a>
            </td>
        </tr>
        </tbody>
{% endfor %}
    </table>
    </div>

{% endblock %}

